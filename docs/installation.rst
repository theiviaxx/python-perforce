============
Installation
============

At the command line::

    $ easy_install python-perforce

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-perforce
    $ pip install python-perforce